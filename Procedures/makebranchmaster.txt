
rem from https://stackoverflow.com/questions/2763006/make-the-current-git-branch-a-master-branch

rem Make sure everything is pushed up to your remote repository (GitHub):
git checkout master

rem Overwrite "master" with "better_branch":
git reset --hard 4.5

rem Force the push to your remote repository:
git push -f origin master

